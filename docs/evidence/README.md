# Evidence

Raw artifacts behind the numbers in `docs/benchmarks.md` and the deck:

- AI Hub profile job outputs (JSON) and screenshots, per model + device
- Any on-device Snapdragon capture sessions (bench output, latency reports)

Naming convention: `<model>__<device>__<date>.<ext>`, e.g.
`whisper-small__x2-elite__2026-09-23.png`.

Nothing here is hand-edited — everything is a direct export or screenshot.
